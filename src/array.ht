#ifndef ARRAY_H
#define ARRAY_H

%INCLUDE%

%TEMPLATE_BEGIN%
typedef struct {
    %TYPE%* items;
    int length;
    int capacity;
} %TYPE%Array;

%TYPE%Array* new%TYPE%Array();

void free%TYPE%Array(%TYPE%Array* a);
%TEMPLATE_END%

#endif // ARRAY_H
