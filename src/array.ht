#ifndef ARRAY_H
#define ARRAY_H

%INCLUDE%

%FORWARD%

%TEMPLATE_BEGIN%
typedef struct {
    %TYPE%* items;
    int length;
    int capacity;
} Array_%TYPENP%;

Array_%TYPENP%* newArray_%TYPENP%();
void freeArray_%TYPENP%(Array_%TYPENP%* a);
%TEMPLATE_END%

#endif // ARRAY_H
