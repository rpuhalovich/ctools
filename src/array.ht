#ifndef ARRAY_H
#define ARRAY_H

%INCLUDE%

%TEMPLATE_BEGIN%
typedef struct {
    %TYPE%* items;
    int length;
    int capacity;
} Array_%TYPENP%;

Array_%TYPE%* newArray_%TYPENP%();
void freeArray_%TYPENP%(Array_%TYPE%* a);
%TEMPLATE_END%

#endif // ARRAY_H
