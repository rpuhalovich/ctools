#include "array_gen.h"

%TEMPLATE_BEGIN%
%TYPE%Array* new%TYPE%Array()
{
    %TYPE%Array* a = malloc(sizeof(%TYPE%Array));
    a->items = malloc(sizeof(%TYPE%));
    a->length = 1;
    a->capacity = 1;
}

void free%TYPE%Array(%TYPE%Array* a)
{
    free(a->items);
    free(a);
}
%TEMPLATE_END%
